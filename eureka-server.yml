server:
  port: 1111   # HTTP port
  
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
    register-with-eureka: false
    fetch-registry: false
